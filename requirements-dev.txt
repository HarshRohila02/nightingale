# Nightingale — test-suite and code-quality dependencies.
#
# CI installs exactly this file, and requirements.txt includes it, so local and CI
# tool versions can never drift apart. Before this file existed, CI installed black
# and ruff unpinned while requirements.txt pinned them — a new black release could
# have turned CI red with no code change (found while applying decision D-2).
#
# Pinned to the versions the codebase is currently formatted and linted with.
# To upgrade a tool: bump it here, run black + ruff locally, commit both together.

# Needed by the test suite itself
pydantic~=2.9
pyyaml~=6.0
# ...and by the DDXPlus parquet builder, which tests/test_ddxplus.py runs end to end.
# They moved here from requirements.txt (2026-09-18) so CI tests the real builder.
pandas~=2.2
numpy~=1.26
pyarrow~=17.0
# ...and by the knowledge-graph store and its tests (task 1b). Moved from requirements.txt.
networkx~=3.3
# ...and by the Neo4j store and its tests (task 1b). Moved from requirements.txt (2026-09-19).
neo4j~=5.25
python-dotenv~=1.0
# ...and by the ML baselines and their tests (task 1c), which fit toy models on synthetic
# rows. Moved from requirements.txt (2026-09-19). scikit-learn brings SciPy with it.
scikit-learn~=1.5
xgboost~=2.1

# Test runner
pytest~=8.3
pytest-cov~=5.0

# Code quality — exact minor versions, because formatting and lint rules change
# between minor releases
black~=26.5.0
ruff~=0.16.0
